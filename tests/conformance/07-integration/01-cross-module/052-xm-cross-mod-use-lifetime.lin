// CATEGORY: integration
// DESCRIPTION: cross mod use lifetime
// EXPECTED: compile_ok
// SOURCE: Stage 11.6+11.7 expansion

mod types{pub struct Ref<'a>{x:&'a i32}} mod app{use types::Ref; pub fn new<'a>(x:&'a i32)->Ref<'a>{Ref{x}}}
fn main(){ let _ref_0 = 0; let _ = Ref{x:&_ref_0}; }
