// CATEGORY: integration
// DESCRIPTION: cross mod use trait method
// EXPECTED: compile_ok
// SOURCE: Stage 11.6+11.7 expansion (Stage 0 limitation)

mod traits{pub trait T{fn f(&self)->i32;}} mod types{pub struct S;} mod app{use traits::T; impl T for types::S{fn f(&self)->i32{0}}}
