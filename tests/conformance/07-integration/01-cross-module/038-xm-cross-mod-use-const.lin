// CATEGORY: integration
// DESCRIPTION: cross mod use const
// EXPECTED: compile_ok
// SOURCE: Stage 11.6+11.7 expansion

mod config{pub const MAX:i32=100;} mod app{use config::MAX; pub fn check(x:i32)->bool{x<MAX}}
fn main(){ let _ = check(0); }
