// CATEGORY: stdlib
// DESCRIPTION: Result enum (stdlib core)
// EXPECTED: compile_ok
// SOURCE: Stage 10.6 conformance expansion

enum Result<T, E> { Ok(T), Err(E) } fn main() { let x = Result::Ok(42); }
