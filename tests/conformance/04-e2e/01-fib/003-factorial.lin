// CATEGORY: e2e
// DESCRIPTION: recursive factorial
// EXPECTED: compile_ok
// SOURCE: Stage 10.4 conformance expansion

fn fact(n: i32) -> i32 { if n <= 1 { 1 } else { n * fact(n - 1) } }
fn main(){ let _ = fact(0); }
