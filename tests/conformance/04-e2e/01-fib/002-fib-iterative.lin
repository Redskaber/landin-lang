// CATEGORY: e2e
// DESCRIPTION: iterative fibonacci
// EXPECTED: compile_ok
// SOURCE: Stage 10.4 conformance expansion

fn fib(n: i32) -> i32 { let mut a = 0; let mut b = 1; let mut i = 0; while i < n { let t = a + b; a = b; b = t; i += 1; } a }
