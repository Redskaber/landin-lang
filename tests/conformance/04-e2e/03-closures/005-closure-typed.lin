// CATEGORY: e2e
// DESCRIPTION: closure with typed params
// EXPECTED: compile_ok
// SOURCE: Stage 10.4 conformance expansion (Stage 0 — compiles but runtime behavior may differ)

fn main() { let f = |x: i32| x * 2; }
