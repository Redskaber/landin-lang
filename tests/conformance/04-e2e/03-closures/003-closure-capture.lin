// CATEGORY: e2e
// DESCRIPTION: closure capturing variable
// EXPECTED: compile_ok
// SOURCE: Stage 10.4 conformance expansion (Stage 0 — compiles but runtime behavior may differ)

fn main() { let a = 1; let b = 2; let f = || a + b; }
