// CATEGORY: e2e
// DESCRIPTION: closure with if expression
// EXPECTED: compile_ok
// SOURCE: Stage 10.4 conformance expansion (Stage 0 — closure compiles but not callable)

fn main() { let f = |x: i32| if x > 0 { 1 } else { 0 }; }
