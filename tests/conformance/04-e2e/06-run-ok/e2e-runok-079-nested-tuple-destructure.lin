// CATEGORY: e2e
// DESCRIPTION: run_ok nested tuple destructuring — Stage 14.49
// EXPECTED: run_ok
// EXPECTED_STDOUT: 1 2 3
// SOURCE: Stage 14.49 — nested tuple destructure `let ((a, b), c) = ((1, 2), 3)` now works

fn main() -> i32 {
    let ((a, b), c) = ((1, 2), 3);
    println!("{} {} {}", a, b, c);
    0
}
