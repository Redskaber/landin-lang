// CATEGORY: e2e
// DESCRIPTION: run_ok struct with tuple field destructure — Stage 14.50
// EXPECTED: run_ok
// EXPECTED_STDOUT: 10 20 99
// SOURCE: Stage 14.50 — struct field with tuple destructure `let Wrapper { data: (a, b), label } = w`

struct Wrapper { data: (i32, i32), label: i32 }
fn main() -> i32 {
    let w = Wrapper { data: (10, 20), label: 99 };
    let Wrapper { data: (a, b), label } = w;
    println!("{} {} {}", a, b, label);
    0
}
