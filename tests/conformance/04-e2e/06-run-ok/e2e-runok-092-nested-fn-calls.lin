// CATEGORY: e2e
// DESCRIPTION: run_ok nested function calls + argument evaluation — Stage 14.54
// EXPECTED: run_ok
// EXPECTED_STDOUT: 26
// SOURCE: Stage 14.54 — nested function call arguments (add(mul(2,3), mul(4,5)))

fn add(a: i32, b: i32) -> i32 { a + b }
fn mul(a: i32, b: i32) -> i32 { a * b }
fn main() -> i32 {
    let result = add(mul(2, 3), mul(4, 5));
    println!("{}", result);
    0
}
