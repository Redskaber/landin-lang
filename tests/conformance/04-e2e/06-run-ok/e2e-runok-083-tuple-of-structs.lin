// CATEGORY: e2e
// DESCRIPTION: run_ok tuple of structs destructure — Stage 14.50
// EXPECTED: run_ok
// EXPECTED_STDOUT: 1 2 3 4
// SOURCE: Stage 14.50 — tuple of structs `let (p1, p2) = (Point{..}, Point{..})` + field access

struct Point { x: i32, y: i32 }
fn main() -> i32 {
    let (p1, p2) = (Point { x: 1, y: 2 }, Point { x: 3, y: 4 });
    println!("{} {} {} {}", p1.x, p1.y, p2.x, p2.y);
    0
}
