// CATEGORY: e2e
// DESCRIPTION: run_ok tuple destructuring in match arm — Stage 14.47
// EXPECTED: run_ok
// EXPECTED_STDOUT: 10 20 30
// SOURCE: Stage 14.47 — match arm tuple destructure now works (was garbage values)

fn main() -> i32 {
    let t = (10, 20, 30);
    match t {
        (a, b, c) => {
            println!("{} {} {}", a, b, c);
            0
        }
    }
}
