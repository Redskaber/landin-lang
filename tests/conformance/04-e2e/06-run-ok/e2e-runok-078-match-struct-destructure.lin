// CATEGORY: e2e
// DESCRIPTION: run_ok struct destructure in match arm — Stage 14.48
// EXPECTED: run_ok
// EXPECTED_STDOUT: 10 20
// SOURCE: Stage 14.48 — struct destructure in match arm (was garbage values)

struct Point { x: i32, y: i32 }
impl Copy for Point {}
fn main() -> i32 {
    let p = Point { x: 10, y: 20 };
    match p {
        Point { x, y } => {
            println!("{} {}", x, y);
            0
        }
    }
}
