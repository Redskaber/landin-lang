// CATEGORY: e2e
// DESCRIPTION: run_ok function with reference param returning reference
// EXPECTED: run_ok
// EXPECTED_STDOUT: 42
// SOURCE: Stage 14.27 — &i32 param + &i32 return + deref verification

fn identity(a: &i32) -> &i32 {
    a
}
fn main() -> i32 {
    let x = 42;
    let r = identity(&x);
    println!("{}", *r);
    0
}
