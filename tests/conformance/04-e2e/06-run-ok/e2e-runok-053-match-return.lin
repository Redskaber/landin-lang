// CATEGORY: e2e
// DESCRIPTION: run_ok match with return inside arms (Stage 14.34 fix)
// EXPECTED: run_ok
// EXPECTED_STDOUT: 100 200 300
// SOURCE: Stage 14.34 — match arm with return now respects is_terminated

fn check(n: i32) -> i32 {
    match n {
        0 => { return 100; }
        1 => { return 200; }
        _ => { }
    }
    300
}
fn main() -> i32 {
    let a = check(0);
    let b = check(1);
    let c = check(99);
    println!("{} {} {}", a, b, c);
    0
}
