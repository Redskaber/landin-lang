//! PASS
//! category: realistic
//! description: iterative fibonacci
//! source: Stage 9.11 conformance expansion
fn fib(n: i64) -> i64 { let mut a = 0; let mut b = 1; let mut i = 0; while i < n { let tmp = a + b; a = b; b = tmp; i += 1; } a }
