//! PASS
//! category: types
//! description: basic dyn trait type
//! source: Stage 9.5 conformance expansion
trait T { fn f(&self); } struct S; impl T for S { fn f(&self) {} } fn g() { let x: dyn T = S; }
