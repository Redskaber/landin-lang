//! FAIL
//! category: types
//! description: nested reference type && (parser limitation — && lexed as AndAnd via maximal munch)
//! error_pattern: parse error
//! source: Stage 9.5 conformance expansion
fn main() { let v: i32 = 1; let r: &i32 = &v; let x: &&i32 = &r; }
