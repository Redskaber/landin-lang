//! PASS
//! category: types
//! description: fn pointer with multiple args
//! source: Stage 9.5 conformance expansion
fn g(a: i32, b: i32) -> i32 { a + b } fn main() { let fp: fn(i32, i32) -> i32 = g; }
