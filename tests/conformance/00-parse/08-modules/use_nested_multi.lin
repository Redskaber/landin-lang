//! PASS
//! category: modules
//! description: multi nested use import
//! source: Stage 9.9 conformance expansion
mod foo { pub struct bar; pub struct baz; pub struct qux; } use foo::{bar, baz, qux};
fn main(){ 0 }
