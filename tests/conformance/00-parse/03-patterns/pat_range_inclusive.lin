//! PASS
//! category: patterns
//! description: inclusive range pattern
//! source: Stage 9.4 conformance expansion
fn f(x: i32) -> i32 { match x { 1..=10 => 1, _ => 0 } }
fn main(){ let _ = f(0); }
