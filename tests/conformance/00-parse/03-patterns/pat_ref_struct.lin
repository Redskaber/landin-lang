//! PASS
//! category: patterns
//! description: reference to struct pattern
//! source: Stage 9.4 conformance expansion
struct P { x: i32, y: i32 } fn f(r: &P) { let &P { x, y } = r; }
impl Copy for P {}
fn main(){ let _ = P{x: 0,y: 0}; }
