//! PASS
//! category: patterns
//! description: nested struct pattern
//! source: Stage 9.4 conformance expansion
struct Inner { x: i32 } struct Outer { inner: Inner } fn f(o: Outer) { let Outer { inner: Inner { x } } = o; }
impl Copy for Inner {}
fn main(){ let _ = Inner{x: 0}; }
