//! PASS
//! category: patterns
//! description: enum path pattern
//! source: Stage 9.4 conformance expansion
enum E { A, B } fn f(e: E) -> i32 { match e { E::A => 1, _ => 0 } }
fn main(){ let _ = E::A; let _ = f(E::A); }
