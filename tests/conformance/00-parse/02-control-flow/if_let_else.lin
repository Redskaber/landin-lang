//! PASS
//! category: control-flow
//! description: if_let_else now supported (Stage 13.2 TD-031 closure)
//! source: Stage 9.3 conformance expansion
enum Opt<T> { Some(T), None } fn f(o: Opt<i32>) -> i32 { if let Opt::Some(x) = o { x } else { 0 } }
fn main(){ let _ = Opt::Some; }
