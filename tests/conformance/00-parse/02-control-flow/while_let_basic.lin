//! PASS
//! category: control-flow
//! description: while_let_basic now supported (Stage 13.2 TD-031 closure)
//! source: Stage 9.3 conformance expansion
enum Opt<T> { Some(T), None } fn main() { let mut it = Opt::Some(1); while let Opt::Some(x) = it { let _ = x; it = Opt::None; } }
