// CATEGORY: soundness
// DESCRIPTION: double mutable borrow should be caught
// EXPECTED: compile_error
// ERROR_PATTERN: borrow
// SOURCE: Stage 10.5 conformance expansion

fn main() { let mut x = 1; let r1 = &mut x; let r2 = &mut x; }
