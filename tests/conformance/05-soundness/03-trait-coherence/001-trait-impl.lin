// CATEGORY: soundness
// DESCRIPTION: basic trait impl
// EXPECTED: compile_ok
// SOURCE: Stage 10.5 conformance expansion (Stage 0 limitation — compiler does not catch this)

trait T { fn f(&self) -> i32; } struct S; impl T for S { fn f(&self) -> i32 { 1 } } fn main() { let s = S; s.f(); }
