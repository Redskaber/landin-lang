// CATEGORY: borrowck
// DESCRIPTION: borrow self in method
// EXPECTED: compile_error
// ERROR_PATTERN: error
// SOURCE: Stage 10.2 conformance expansion (Stage 0 limitation)

struct S { x: i32 } impl S { fn get(&self) -> i32 { self.x } fn set(&mut self, v: i32) { self.x = v; } }
