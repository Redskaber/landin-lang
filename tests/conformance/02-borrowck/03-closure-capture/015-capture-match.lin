// CATEGORY: borrowck
// DESCRIPTION: closure with match
// EXPECTED: compile_error
// ERROR_PATTERN: error
// SOURCE: Stage 10.2 conformance expansion (Stage 0 limitation)

fn main() { let x = 1; let f = || match x { 0 => 1, _ => 0 }; let _ = f(); }
