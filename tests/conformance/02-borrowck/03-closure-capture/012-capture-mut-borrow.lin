// CATEGORY: borrowck
// DESCRIPTION: closure captures mutable borrow
// EXPECTED: compile_error
// ERROR_PATTERN: error
// SOURCE: Stage 10.2 conformance expansion (Stage 0 limitation)

fn main() { let mut x = 1; let r = &mut x; let f = || { *r = 2; }; f(); }
