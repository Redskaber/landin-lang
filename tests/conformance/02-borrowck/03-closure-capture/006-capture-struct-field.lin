// CATEGORY: borrowck
// DESCRIPTION: closure captures struct field
// EXPECTED: compile_error
// ERROR_PATTERN: error
// SOURCE: Stage 10.2 conformance expansion (Stage 0 limitation)

struct S { x: i32 } fn main() { let s = S { x: 1 }; let f = || s.x; let _ = f(); }
