// CATEGORY: borrowck
// DESCRIPTION: copy type (i32) doesn't move
// EXPECTED: compile_error
// ERROR_PATTERN: error
// SOURCE: Stage 10.2 conformance expansion (Stage 0 limitation)

fn main() { let x = 1; let y = x; let z = x; }
