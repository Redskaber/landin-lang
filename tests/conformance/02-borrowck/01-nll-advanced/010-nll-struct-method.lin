// CATEGORY: borrowck
// DESCRIPTION: NLL with struct method call
// EXPECTED: compile_error
// ERROR_PATTERN: error
// SOURCE: Stage 10.2 conformance expansion (Stage 0 limitation)

struct S { x: i32 } impl S { fn get(&self) -> i32 { self.x } } fn main() { let s = S { x: 1 }; s.get(); }
