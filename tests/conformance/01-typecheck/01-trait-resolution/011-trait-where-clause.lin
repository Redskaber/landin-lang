// CATEGORY: typecheck
// DESCRIPTION: trait bound via where clause
// EXPECTED: compile_error
// ERROR_PATTERN: no method
// SOURCE: Stage 10.1 conformance expansion (Stage 0 limitation — compiler does not fully support this feature)

trait Clone { fn clone(&self) -> i32; } fn f<T>(x: T) -> i32 where T: Clone { x.clone() }
fn main(){ 0 }
