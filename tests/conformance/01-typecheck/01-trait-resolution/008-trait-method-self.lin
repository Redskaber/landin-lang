// CATEGORY: typecheck
// DESCRIPTION: trait method with &self
// EXPECTED: compile_ok
// STAGE_13.17: Flipped from compile_error (self binding fix)
// SOURCE: Stage 10.1 conformance expansion (Stage 0 limitation — compiler does not fully support this feature)

trait Show { fn display(&self) -> i32; } struct Foo { x: i32 } impl Show for Foo { fn display(&self) -> i32 { self.x } }
fn main(){ let _ = Foo{x: 0}; }
