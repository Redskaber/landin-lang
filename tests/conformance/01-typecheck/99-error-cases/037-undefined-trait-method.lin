// CATEGORY: typecheck
// DESCRIPTION: calling undefined trait method
// EXPECTED: compile_ok
// SOURCE: Stage 10.8 typecheck expansion (Stage 0 limitation — typeck does not catch this)

trait T { fn f(&self); } struct S; impl T for S { fn f(&self) {} } fn main() { let s = S; s.g(); }
