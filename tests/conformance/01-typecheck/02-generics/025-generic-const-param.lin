// CATEGORY: typecheck
// DESCRIPTION: generic with const-like usage
// EXPECTED: compile_error
// ERROR_PATTERN: error
// SOURCE: Stage 10.8 typecheck expansion (Stage 0 limitation)

fn f<T>(x: T) -> T { x } fn main() { let a = f(1); let b = f(true); }
