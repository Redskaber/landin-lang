// CATEGORY: typecheck
// DESCRIPTION: generic trait
// EXPECTED: compile_ok
// SOURCE: Stage 10.1 conformance expansion

trait T<U> { fn f(&self, x: U) -> i32; } struct S; impl T<i32> for S { fn f(&self, x: i32) -> i32 { x } }
fn main(){ let _ = S; }
