// CATEGORY: typecheck
// DESCRIPTION: generic function in const context
// EXPECTED: compile_error
// ERROR_PATTERN: error
// SOURCE: Stage 10.1 conformance expansion (Stage 0 limitation — compiler does not fully support this feature)

fn id<T>(x: T) -> T { x } const C: i32 = 42; fn main() { let _ = id(C); }
