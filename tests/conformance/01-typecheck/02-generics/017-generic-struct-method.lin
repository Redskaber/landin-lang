// CATEGORY: typecheck
// DESCRIPTION: method on generic struct returning T
// EXPECTED: compile_error
// ERROR_PATTERN: error
// SOURCE: Stage 10.1 conformance expansion (Stage 0 limitation — compiler does not fully support this feature)

struct S<T> { x: T } impl<T> S<T> { fn new(x: T) -> Self { S { x } } }
fn main(){ 0 }
