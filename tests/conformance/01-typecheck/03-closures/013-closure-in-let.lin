// CATEGORY: typecheck
// DESCRIPTION: closure assigned to let
// EXPECTED: compile_error
// ERROR_PATTERN: error
// SOURCE: Stage 10.1 conformance expansion (Stage 0 limitation — compiler does not fully support this feature)

fn main() { let g = |x: i32| x + 1; let r = g(5); }
