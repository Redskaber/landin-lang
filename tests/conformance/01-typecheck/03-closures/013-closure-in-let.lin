// CATEGORY: typecheck
// DESCRIPTION: closure assigned to let
// EXPECTED: compile_ok
// SOURCE: Stage 10.1 conformance expansion (Stage 0 limitation — compiler does not fully support this feature) (Stage 13.3a closure call lowering — TD-030 closed)

fn main() { let g = |x: i32| x + 1; let r = g(5); }
