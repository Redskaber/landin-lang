# v0.3 完整设计文档 — 路线图与架构决策

> **Author**: redskaber
> **Date**: 2026-08-04 (Stage 16.44 最终更新 — RELEASE SIGNED OFF)
> **Version**: v0.234.2
> **Process**: stage-committee-process.md v3.24 §13.4 + §25.8 design-writeback
> **Status**: v0.3 RELEASE SIGNED OFF — 闭包重设计 100% 完成 + codegen 架构重构完成

## 1. Executive Summary

v0.3 是 Landin 编译器的第三个版本，聚焦于**健全性**和**架构基础**。
本文档是 v0.3 的完整设计文档，包含所有已完成的工作、进行中的任务、
以及未来规划（原 v0.4 规划已合并到 v0.3）。

### v0.3 核心目标

1. **Sound Copy 检测** ✅ 完成 — 字段级推导，镜像 Rust `#[derive(Copy)]`
2. **Task 3: TraitResolver Keys** ✅ 完成 — DefId 键控查找，类型安全
3. **Task 10: 闭包重设计** ✅ 100% 完成 — 所有 5 步完成，所有闭包模式工作
4. **Codegen 架构重构** ✅ 完成 — 统一管道、后端分离、零死代码
5. **Task 11: 单态化** ✅ 完成 — 9 个阶段全链路实现（Stages 16.49-16.59）
6. **Task 14: 对象安全** ✅ 完成 — 5 条规则检查 + driver 集成（Stages 16.64-16.65）
7. **Task 17: 关联类型** ✅ 完成 — 4 个阶段全链路实现（Stages 16.67-16.69）

## 2. 已完成工作（Stages 16.00–16.18）

### 2.1 Sound Copy 检测（Stages 15.99, 16.02–16.06）

**问题**: `ty_is_copy` 对所有 Adt 类型返回 `true`（不健全）。

**解决方案**: 字段级 Copy 推导
- `TraitResolver.derived_copy_types: HashSet<DefId>` — 所有字段都是 Copy 且无 `impl Drop` 的结构体/枚举
- 不动点迭代处理嵌套结构体
- `is_copy_builtin()` 先检查 `derived_copy_types`，再检查显式 `impl Copy`
- `ty_is_copy` 标记 `#[deprecated]`
- MIR lowerer 使用 `Operand::Move`（替代 `Operand::Copy`）用于 let 绑定、返回、调用参数

**测试**: +10 集成测试 + 3 个现有测试更新

### 2.2 Task 3: TraitResolver Keys（Stages 16.07–16.11）

**问题**: `impl_by_trait_and_type: HashMap<(Spur, Spur), DefId>` 键不类型安全，不支持泛型。

**解决方案**: DefId 键控查找
- `impls_by_def_ids: HashMap<(DefId, DefId), DefId>` — DefId 键控的 impl 查找
- `vtables_by_def_ids: HashMap<(DefId, DefId), Vtable>` — DefId 键控的 vtable 查找
- `populate_def_id_keyed_maps()` 后置处理 — 解决 HIR 迭代顺序问题
- `find_impl_by_def_ids()`, `implements_by_def_ids()`, `find_vtable_by_def_ids()` — 新查询方法
- `find_trait_def_id()`, `impl_methods_by_def_ids()` — 辅助方法
- 5 个 Spur 键控方法标记 `#[deprecated]`

**测试**: +33 集成测试

### 2.3 Task 10: 闭包重设计 Steps 1+2（Stages 16.13–16.14）

**问题**: 内联闭包调用方式有代码膨胀、无优化、MIR 污染等问题。

**解决方案**: Strategy A（合成 `call` 函数）基础设施
- `SynthesizedClosureFunction` 结构体 — 闭包元数据
- `synthesized_closure_functions` side-table on `MirLowerCtxt`
- `allocate_closure_def_id()` — 唯一 DefId 分配（修复共享 DefId bug）
- `build_synthesized_closure_mir_body()` — 构建独立 MirBody
- `synthesized_closure_mir_bodies` field on `CompileResult`
- `MirBody.def_id` — 永久改进，用于 codegen 函数名解析

**测试**: +24 集成测试

### 2.4 深度审查（4 轮）

- Round 1 (Stage 16.09): v0.3 基础审查 — GO
- Round 2 (Stage 16.12): Task 3 完成后审查 — GO
- Round 3 (Stage 16.15): Task 10 Steps 1+2 审查 — GO
- Round 4 (Stage 16.18): v0.3 发布就绪审查 — **RELEASE APPROVED**

## 3. 进行中/规划中的工作

### 3.1 Task 10 Steps 3+4: 闭包切换（需要深度 codegen 变更）

**状态**: 尝试两次（Stages 16.16–16.17），已回退

**MIR 侧（完成）**: `lower_closure_call_to_synthesized()` 正确发射 `TerminatorKind::Call`

**Codegen 侧（需要修复）**:
1. **返回类型**: 合成 MIR body 的 LocalId(0) 有 Infer 类型（typeck 不在其上运行）
   - 需要: 在合成 MIR body 上运行 typeck，或从闭包 body 表达式类型推断返回类型
2. **self 参数**: Codegen 将 Closure 结构体当作 i32，而非指针
   - 需要: codegen 对 Closure 类型发射 OpaquePtr
3. **调用点参数传递**: 传递 `{} 0` 而非闭包结构体指针
   - 需要: 调用点按指针传递闭包结构体

**计划**: 作为 v0.3 的后续 stage，聚焦 codegen 重构

### 3.2 Task 11: 单态化 ✅ 完成（Stages 16.49–16.59）

**目标**: 真正的泛型实例化 + 代码生成

**前置条件**: 泛型解析器支持（`Vec<T>` 语法）— ✅ 已确认（parser→AST→HIR→MIR 全链路支持）

**依赖**: Task 3（已完成 ✅）

**实现状态**: 9 个阶段全链路完成，Deep Review Round 9 GO

**实现总结**:

| Phase | Stage | 描述 | 状态 |
|-------|-------|------|------|
| 1a | 16.50 | `generics_of` 查询 — DefId → Vec<ParamTy> | ✅ |
| 1b | 16.51 | Substs 传播到 `TyKind::Adt`（类型注解） | ✅ |
| 1c | 16.52 | Substs 传播到 `AggregateKind::Adt`（字面量构造） | ✅ |
| 2 | 16.53 | `substitute(ty, substs)` 类型替换函数 + 字段类型解析集成 | ✅ |
| 3 | 16.54 | `collect_mono_items` — 遍历 MIR，收集+去重 MonoItem | ✅ |
| 4a | 16.55 | 特化命名 `mangle_ty` / `mono_item_name` / `build_mono_item_names` | ✅ |
| 4b-pre | 16.56 | 嵌套泛型参数解析（`lookup_type_def_id_by_name`） | ✅ |
| 4b | 16.57 | 单态化布局 `MonoLayoutKey` / `MonoLayoutMap` / `build_mono_layouts` | ✅ |
| 4c | 16.58-16.59 | Codegen 集成 `lookup_mono_layout` / `mir_type_to_emit_type_with_layouts_and_mono` | ✅ |

**关键架构决策**:
1. **SubstsRef = Rc<[Ty]>** — interned slice，clone 是 refcount bump
2. **TyKind::Param(ParamTy)** — 泛型参数在 MIR 中的表示
3. **substitute 是纯函数** — 无 HIR 访问，无副作用
4. **MonoLayoutKey 用 Vec<TyKind>** — Rc<[Ty]> 不实现 Hash/Eq
5. **mono_layouts: Option<&MonoLayoutMap>** — 向后兼容（None 时回退到 AdtLayouts）
6. **Phase 4c 管道集成** — run_codegen_pipeline 构建 MonoLayoutMap，全链路传递到所有 codegen 子模块

**验证**: 8071 tests passing (343 lib + 2504 integration + 5224 conformance), 0 failures, 0 warnings

### 3.3 Task 14: 对象安全 ✅ 完成（Stages 16.64–16.65）

**目标**: 检查 trait 是否对象安全（`dyn Trait` 可用）

**规则**: trait 不能有 `Self` 类型的方法返回值、泛型方法等

**依赖**: Task 11（需要泛型类型信息）— ✅ 已就绪

**实现状态**: Phase 1+2 完成

| Phase | Stage | 描述 | 状态 |
|-------|-------|------|------|
| 1 | 16.64 | `ObjectSafetyViolation` 枚举 + `check_trait_object_safety` 检查器 + 10 单元测试 | ✅ |
| 2 | 16.65 | Driver 集成 — 扫描 `dyn Trait` 使用，发出 typeck 错误 + 8 集成测试 | ✅ |

**5 条对象安全规则**:
1. SelfReturn — 方法返回 `Self` 类型
2. SelfInArg — 方法参数包含 `Self`
3. GenericMethod — 方法有泛型类型参数
4. NoReceiver — 方法没有 `self`/`&self`/`&mut self` 接收者
5. ByValueReceiver — 方法按值接收 `self`

**验证**: 8099 tests passing (353 lib + 2522 integration + 5224 conformance), 0 failures, 0 warnings

### 3.4 Task 17: 关联类型 ✅ 完成（Stages 16.67–16.69）

**目标**: `type Item;` 在 trait 中定义关联类型，impl 中解析

**依赖**: Task 11（需要泛型基础设施）— ✅ 已就绪

**实现状态**: 4 个阶段全链路完成

| Phase | Stage | 描述 | 状态 |
|-------|-------|------|------|
| 1 | 已有 | AST + HIR 解析（`HirAssocType`, `HirTraitItem::Type`） | ✅ |
| 2 | 16.67 | MIR `TyKind::Projection(DefId, SubstsRef)` — 8 个匹配点更新 | ✅ |
| 3 | 16.68 | `projection_resolver` 模块 — 解析算法（查找 trait → impl → 具体类型） | ✅ |
| 4 | 16.69 | Driver 集成 — `resolve_projections_in_mir` 在 typeck 后调用 | ✅ |

**关键架构决策**:
1. **TyKind::Projection(DefId, SubstsRef)** — MIR 中表示未解析的关联类型
2. **projection_resolver 纯函数模块** — 无 HIR 访问在 typeck 内部，在 driver 中调用
3. **递归解析** — 处理嵌套投影（Ref/Ptr/Slice/Array/Tuple/Adt 内的 Projection）
4. **优雅降级** — 无法解析时保留 Projection（不崩溃）

**验证**: 8106 tests passing (353 lib + 2529 integration + 5224 conformance), 0 failures, 0 warnings

## 4. v0.3 完整路线图（最终版 — Stage 16.44）

| 优先级 | 项目 | 状态 | 阶段 |
|--------|------|------|------|
| P1 | Sound Copy 检测 | ✅ 完成 | 15.99, 16.02-16.06 |
| P1 | Task 3: TraitResolver Keys | ✅ 完成 | 16.07-16.11 |
| P2 | Task 10 Steps 1+2: 闭包基础设施 | ✅ 完成 | 16.13-16.14 |
| P2 | Task 10 Steps 3+4: 闭包切换 (通解) | ✅ 完成 | 16.16-16.32 (含 typeck gap 修复) |
| ✅ | Task 10 Step 5: 清理内联路径 | ✅ 完成 | 16.34 |
| ✅ | Codegen 架构重构 | ✅ 完成 | 16.35-16.42 |
| ✅ | 深度审查 Round 6-8 | ✅ GO | 16.33, 16.39, 16.43 |
| ✅ | Codegen 文档定稿 | ✅ 完成 | 16.41 |
| P3 | 泛型解析器支持 | 🔧 规划中 | — |
| P3 | Task 11: 单态化 | ✅ 完成（Stages 16.49-16.59, Round 9 GO）| — |
| P3 | Task 14: 对象安全 | ✅ 完成（Stages 16.64-16.65）| — |
| P3 | Task 17: 关联类型 | ✅ 完成（Stages 16.67-16.69）| — |
| P4 | Task 4: EmitValue 类型化句柄 | 🔧 延后 | — |
| P4 | Emitter trait 物理拆分 | 🔧 延后 | 16.38 |

## 5. 架构决策记录

### 5.1 字段级 Copy 推导（Stage 16.06）

**决策**: 不迁移 199 个测试文件添加 `impl Copy`，而是实现字段级推导。

**理由**: 
- 用户友好（`struct Point { x: i32, y: i32 }` 直觉上是 Copy）
- 健全（保守推导，只有所有字段都是 Copy 且无 `impl Drop` 才推导）
- §16 合规（TraitResolver 读取 HIR，BorrowChecker 查询无需 HIR）

### 5.2 DefId 键控查找（Stage 16.07）

**决策**: 并行 DefId 键控 map，渐进迁移，最终废弃 Spur 键控方法。

**理由**:
- 类型安全（DefId 是唯一标识符，非字符串哈希）
- 无需 interner（调用者不需要 `&Rodeo`）
- 为泛型 SubstsRef 支持做准备

### 5.3 闭包 Strategy A（Stage 16.13）

**决策**: 合成 `call` 函数，而非内联闭包体。

**理由**:
- 匹配设计文档 `07-codegen.md` §8.1-8.2
- 匹配 Rust 的方法
- 更清晰的 MIR，支持优化

### 5.4 MirBody.def_id（Stage 16.17）

**决策**: 在 MirBody 上添加 `def_id: Option<DefId>` 字段。

**理由**:
- 永久改进，用于 codegen 函数名解析
- 替代脆弱的字符串搜索
- §16 合规（数据携带在 IR 上，不从 HIR 查找）

## 6. 技术债清单（最终版 — Stage 16.44）

| ID | 描述 | 优先级 | 状态 |
|----|------|--------|------|
| TD-CLOSURE-1 | 内联闭包调用路径 | P2 | ✅ 修复 (16.29) |
| TD-CLOSURE-2 | `closure_bodies` 与 `synthesized_closure_functions` 重复 | P3 | ✅ 修复 (16.34) |
| TD-CLOSURE-CODEGEN-1 | 嵌套闭包运行时代码生成 | P2 | ✅ 修复 (16.30) |
| TD-CLOSURE-BORROWCK-1 | 闭包 MIR body 上的借用检查 | P2 | ✅ 修复 (16.31) |
| TD-CLOSURE-TRIPLE-1 | 三重嵌套闭包类型检查 | P3 | ✅ 修复 (16.32) |
| TD-CODEGEN-1 | 编译错误 (cfg gate) | P0 | ✅ 修复 (16.35) |
| TD-CODEGEN-2 | text-backend 工具函数位置错误 | P1 | ✅ 修复 (16.35) |
| TD-CODEGEN-3 | 死代码 (emit_output 等) | P2 | ✅ 修复 (16.35-16.36) |
| TD-CODEGEN-4 | 分叉入口点 | P2 | ✅ 修复 (16.37) |
| TD-CODEGEN-5 | Emitter trait 膨胀 (39 方法) | P3 | 🔧 延后 (16.38) |
| TD-CODEGEN-6 | `EmitValue = String` 泄漏 text-IR 假设 | P3 | 🔧 延后 |
| TD-COPY-1 | `ty_is_copy` 废弃（仅测试用）| P3 | ✅ 已废弃 |
| TD-FALLBACK-1 | `BorrowChecker::new()` 不健全（仅测试用）| P3 | ✅ 已记录 |
| TD-MIR-COPY-1 | `is_mir_ty_copy_conservative` 对 Adt 返回 false | P3 | ✅ 已记录 |

**所有可行的技术债已关闭。** 剩余 P3 项目需要大规模代码移动或 API 重设计。

## 7. 测试统计（最终版 — Stage 16.44）

| 测试类型 | 数量 | 通过率 |
|---------|------|--------|
| Lib tests | 244 | 100% |
| Integration tests | 2402 | 100% |
| Conformance tests | 5224 | 100% |
| **Total** | **7870** | **100%** |

v0.3 新增测试: +250 stage-16 集成测试 (28 个测试文件)

### Codegen 代码质量指标

| 指标 | 值 | 状态 |
|------|-----|------|
| codegen `#[allow(dead_code)]` | 0 | ✅ |
| codegen `#[allow(unused_imports)]` | 0 | ✅ |
| TODO/FIXME/HACK | 0 | ✅ |
| 死代码 | 0 | ✅ |
| 废弃项 | 15 (全部有 note) | ✅ |

## 8. Codegen 架构重构总结（Stages 16.35-16.42）

### 8.1 统一管道 (Stage 16.37)

**决策**: 提取 `run_codegen_pipeline(result, &mut dyn Emitter)` 函数。

**理由**: 消除 text 和 LLVM 后端的重复入口逻辑和反转的发射顺序。

### 8.2 后端分离 (Stage 16.35)

**决策**: 将 text-backend 工具函数 (`emit_type_to_llvm_str`, `binop_to_llvm_str`) 从共享 `emitter.rs` 移到 `text/mod.rs`。

**理由**: 这些函数只被 `TextEmitter` 使用。LLVM C-API 后端有自己的 `llvm_type()` 方法。

### 8.3 死代码清理 (Stages 16.35-16.40)

**决策**: 移除所有死代码和死重导出。

**移除项**: `emit_output`, `emit_dyn_trait_ptr_type`, `llvm_ptr_str`, `to_context`, `predeclare_function`, 7 个 `dyn_trait_emit` 死重导出, 8 个 `#[allow(unused_imports)]` 注解。

### 8.4 文档分组 (Stage 16.38)

**决策**: Emitter trait 方法组织为 3 个文档组 (Module-level, Function scope, Local state)。

**理由**: 物理拆分被 Rust 的单 impl 块规则阻止，文档分组提供架构清晰度。

## 9. 参考文献

- v0.3 启动: `docs/develop/v0/stage-16/stage-16.00-v0.3-kickoff.md`
- Task 3 设计: `docs/develop/v0/task-3-traitresolver-keys-design.md`
- Task 10 设计: `docs/develop/v0/task-10-closure-redesign-design.md`
- 深度审查: `docs/develop/v0/stage-16/deep-review-round{1-8}.md`
- API 命名标准: `docs/develop/v0/api-naming-standard.md`
- 架构决策: `docs/develop/v0/architecture-decisions.md`
- Codegen 架构图: `docs/graph/codegen/`
- LLVM 后端文档: `docs/llvm/backend-architecture.md`
- Stage 委员会流程: `docs/stage-committee-process.md`
