# Auto-generated by scripts/switch-llvm-version.sh
# LLVM version: 22 (llvm-sys 221)

[build]
# Stage 18.311: LLVM 22 (llvm-sys 221) is the default since Stage 18.210.
# LLVM 19.x is the build-server fallback (via switch-llvm-version.sh 19).

[env]
LLVM_SYS_221_PREFIX = "/tmp/llvm-22-prefix"
LLVM_LINK_SHARED = "1"
